name: server
on: workflow_dispatch
jobs:
  server:
    runs-on: ubuntu-24.04-arm
    steps:
      - run: |
          sudo apt-get install hping3
          curl -s http://ip-api.com/line/
          sudo hping3 -S -p 23 -c 4 195.208.208.232
