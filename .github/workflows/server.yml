name: server
on: workflow_dispatch
jobs:
  server:
    runs-on: ubuntu-24.04-arm
    steps:
      - run: |
          git clone https://github.com/apersomany/libcfd.git
          cd libcfd
          rustup update
