name: server
on: workflow_dispatch
jobs:
  server:
    runs-on: ubuntu-24.04-arm
    steps:
      - run: |
          sudo apt-get install mtr
          curl -s http://ip-api.com/line/
          sudo mtr -T -P 443 -r -c 20 87.226.162.216
