name: Benchmark Throughput Parallel

on:
  push:
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ubuntu-24.04-arm
    steps:
      - run: |
          sudo apt-get install -y wrk
          wrk -t4 -c750 -d60s http://lscf.eu.cc/
